import { CompilerConfig } from '@ton/blueprint';

export const compile: CompilerConfig = {
    lang: 'tact',
    // target: 'contracts/escrow.tact',
    // target: 'contracts/tokens.tact',
    target: 'contracts/nfts.tact',
    // target: 'contracts/kinem_escrow.tact',
};
